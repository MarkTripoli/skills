# gate=true: the `cycle` loop reads the previous pass's intent, runs the phase and its approval gate;
# `once` is skipped.
intent: '{"intent":"revise"}'
phase: "artifact saved"
fixture:
  expect: completed
  inputs: { task_dir: .agents/tasks/fixture, skill: create-plan, iterate: iterate-plan, gate: "true" }
  reached: [intent, phase, cycle]
exec-code: false
