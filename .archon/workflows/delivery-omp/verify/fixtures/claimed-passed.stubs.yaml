# The session claims `passed` but the typed-judgment cross-check reads a failed item in the artifact:
# the loop and `iterate-verify` follow the checked status, not the claim.
verify-implementation: '{"status":"passed","artifact":"06-verification-fixture.md","summary":"all items passed"}'
verification-status: '{"status":"failed"}'
iterate-verify: "implementation revised for the item the table marks failed"
verification-done: ""
fixture:
  expect: completed
  inputs: { task_dir: .agents/tasks/fixture }
  reached: [verify-implementation, verification-status, iterate-verify, verification-done, verification]
exec-code: false
