# `gates=none` and a confident route: no confirmation; the judged pick was not explicit, so `route`
# (CR-001) already names the pack as `adaptive`, and `resolve` reuses it instead of the fixed oneshot
# pack; task.md still names oneshot. The run reaches the adaptive child alone (a dry run cannot start a
# `workflow:` child, so it fails there; the seven other packs are skipped).
route: '{"workflow":"oneshot","gates":"none","suggested":"oneshot","confidence":"0.95","confirm":"false","explicit":"false","gates_plan":"false","pack":"adaptive"}'
resolve: '{"workflow":"oneshot","gates":"none","pack":"adaptive"}'
task__create: '{"task_dir":".agents/tasks/fixture","skills_dir":"/home/me/.agents/skills","tier":"","suggested_workflow":""}'
fixture:
  expect: failed
  fail-node: adaptive
  inputs: { gates: none }
  reached: [route, resolve, task__create]
exec-code: false
