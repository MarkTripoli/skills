---
task: steer-every-archon-gate
type: verification
summary: "Re-ran both repository checks (`npm test`, 69/69; the CI commit check, ok on 66 subjects) and every acceptance item plan 13 and the receipts promise, including the live-run evidence receipt 18 deferred: two scratch `delivery-start` runs were paused at `confirm`, the steward's own state read and `respond ... --detach --cwd \"$cwd\"` were run verbatim from a directory outside each run's worktree, and both gates resolved (`approved`, and `rejected` whose words switched the child pack to `delivery-lean`). The three revert checks confirm `tests/steward.test.mjs` fails when the guard, `--cwd`, or `herd-next`'s guard is removed. Status is passed: no item failed, and the untested items are acceptance (a) and (b), the agent behavior inside and outside Herdr, which no command in this session could decide. The review phase should read the two Known limits that change nothing in behavior but misstate it: `deliver/SKILL.md:78`'s cause sentence, and `epic_delivery_final_answer.md`'s line telling a person to run the child start commands."
status: passed
revision: e3aa445
target: main
---

# Verification

## Run

- Revision: `e3aa445` on `herdr-plugin-delivery-flow`; tracked tree clean, two untracked paths present before this session (`.backups/`, `.ignore`)
- Target: `main` (the base of pull request #19); 90 files changed, 5 of them tests
- Checks from: `package.json` `test` script, and `.github/workflows/commits.yml` (`scripts/check-commits.mjs`); `release.yml` runs `npm ci` only and is a deployment step
- Coverage: 15 acceptance items; 8 claimed by a receipt, 7 claimed by none
- Graded by: typed-judgment helper (`grade-steps --kind command`, then `--kind diff`); model `jev-1.13.0`, tokens `4685` in / `428` out and `2161` in / `179` out

## Items

| Id | Item | Decided by | Expected | Observed | Verdict | Confidence | Severity |
|---|---|---|---|---|---|---|---|
| C1 | The repository's test script. | `npm test` | Exits 0 with no failing test. | Exit 0; `tests 69 / suites 3 / pass 69 / fail 0 / skipped 0 / todo 0`, duration 30966ms; the five steward cases are in the pass list. | pass | 0.96 | 0 |
| C2 | The CI Commits job. | `node scripts/check-commits.mjs main..HEAD --title "feat(delivery): open the next delivery phase in a Herdr pane"` | Exits 0; every subject and the pull request title are conventional. | Exit 0; `ok: 66 subjects`. | pass | 1.00 | 0 |
| T1 | `tests/steward.test.mjs` (new, +102). | `git diff main...HEAD -- tests/steward.test.mjs`, read in this session | The change keeps this check's strength. | 5 tests added, none removed or skipped; extracts the three bash fences by marker and runs them against a fake `archon`; each assertion shown load-bearing by the reverts under A9. | pass | 0.91 | 0 |
| T2 | `tests/judge.test.mjs` (+93/-5). | `git diff main...HEAD -- tests/judge.test.mjs`, read in this session | The change keeps this check's strength. | The 5 removed lines are rewrites: three assertions gained an explicit `NO_KEY` env so no key file can satisfy them, and the expected stderr grew `and no key file was found`. New cases cover retry, axis coverage, and the unclear fallback. No skip marker. | pass | 0.87 | 0 |
| T3 | `tests/commits.test.mjs` (+5). | `git diff main...HEAD -- tests/commits.test.mjs`, read in this session | The change keeps this check's strength. | Purely additive: `--title` alone is a valid invocation, a 74-character title exits 1 with the limit message, a short one exits 0. | pass | 0.94 | 0 |
| T4 | `tests/build-packs.test.mjs` (+3/-1). | `git diff main...HEAD -- tests/build-packs.test.mjs`, read in this session | The change keeps this check's strength. | The unkeyed case now also sets `TYPESAFE_API_KEY_FILE` to a missing path, so this machine's key file cannot satisfy the no-key assertion; the assertions are unchanged. A tightening, not a loosening. | pass | hand | 0 |
| T5 | `tests/lib/typesafe-stub.mjs` (+9/-1). | `git diff main...HEAD -- tests/lib/typesafe-stub.mjs`, read in this session | The change keeps this check's strength. | The fixture server gained an optional `statuses` queue and `retry-after` header; the signature change is a new optional argument, nothing removed. | pass | 0.90 | 0 |
| A1 | Acceptance (a): `/deliver` inside Herdr leaves a review pane whose agent announces the first pause and asks for a decision, no `archon` command in any reply (`task.md:13`; claimed: no). | Not in this environment: the pane half is agent behavior in a live Herdr workspace. `HERDR_ENV` is `1` here, but opening a review pane and starting an agent in the person's workspace is an outward-facing action this session was not asked to take. | A pane labelled `<slug>/<phase> gate` at the run's `working_path`, its agent announcing the pause unprompted. | Not run. The primitives it calls exist (A14) and the reply it prints carries no command (A10). | untested | hand | 0 |
| A2 | Acceptance (b): the same outside Herdr, in the starting session (`task.md:13`; claimed: no). | Not in this environment: a live `/deliver` session's own prose is not a command whose output can be captured. | The session prints `deliver_gate_answer.md` and ends with the ask. | Not run. Its CLI half is A3, A4, and A6; its template half is A10. | untested | hand | 0 |
| A3 | Acceptance (c), the approve path (`task.md:13`; claimed: yes, receipt 09). | `archon workflow respond "$run_id" "$decision" "$text" --detach --cwd "$cwd"`, decision `approve`, run from this repository root against live paused run `6bd7f01d` | The gate resolves and the run continues. | `Started 'approve' for run 6bd7f01d-... in the background.`, exit 0; four seconds later `status=running resolved=approved`; the run dispatched its routed child `delivery-full`. | pass | 0.90 | 0 |
| A4 | Acceptance (c), describing a change in words (`task.md:13`; claimed: no). | The same call with decision `reject` and text `lean, outline`, against live paused run `6d86fa7a` | The gate resolves with the person's words as the text and the change takes effect. | `Started 'reject' for run 6d86fa7a-... in the background.`, exit 0; then `status=running resolved=rejected`; the child run it dispatched was `delivery-lean`, not the routed `delivery-full`. | pass | 0.91 | 0 |
| A5 | The reply mapping the steward uses when the answer is not one plain word (`13-plan:47`, `deliver/SKILL.md:96-99`; claimed: no). | `node skills/delivery/typed-judgment/judge.mjs feedback-intent --json -` on two replies | `proceed` for an approval, `revise` for a change request. | `looks good to me, go ahead` -> `{"intent":"proceed","suggested":"proceed","confidence":1}`; `Use the lean pack and only keep the outline gate` -> `{"intent":"revise","suggested":"revise","confidence":1}`; both exit 0, `judge: model jev-1.13.0`. | pass | 0.91 | 0 |
| A6 | The steward's state read against a real paused run (`13-plan:37`; claimed: yes, receipt 09). | The bash fence extracted verbatim from `deliver/SKILL.md:69-77`, run from this repository root | `status`, `working_path`, the node, and every decision id are read, never guessed. | `status=paused`, `cwd=/Users/.../worktrees/archon/task-delivery-start-1789748368309`, `node=confirm`, `decisions=approve reject`, `resolved=[]`, the gate `msg` in full; fence exit 0. | pass | 0.92 | 0 |
| A7 | Desired End State: a steward that cannot read its run reports and stops (`13-plan:35`; claimed: yes, receipts 14 and 16). | The same fence run from `/tmp`, not a git work tree | Archon's output is printed as cause and fix and nothing downstream runs. | Printed `{ "ok": false, "error": "Error: Not in a git repository.\nThe Archon CLI must be run from within a git repository...` and exited 1; the line after the read never ran. | pass | 0.89 | 0 |
| A8 | Desired End State: every call after the first read names the run's own directory (`13-plan:36`; claimed: yes, receipt 14). | `grep -F '--cwd "$cwd"' skills/delivery/deliver/SKILL.md`, then the three live calls | The flag is on `respond`, `wait`, and `get`, and the live CLI accepts it from outside the run's worktree. | `:106` respond, `:108` wait, `:109` get, `:115` prose; live from another repository's root, `get --cwd` returned the full paused JSON, `wait --timeout 5 --cwd` returned `{"result":"deadline","observedStatus":"running"}`, `respond --detach --cwd` exited 0 and resolved the gate. | pass | 0.84 | 0 |
| A9 | Desired End State: the test fails when the guard or `--cwd` is reverted (`13-plan:37`, `13-plan:282`; claimed: yes, receipt 16). | Three reverts in a throwaway `git worktree` at HEAD, then `node --test tests/steward.test.mjs` | Each revert fails exactly one named test. | Guard stripped: `✖ a get that fails ends the read ...` (5/4/1). `--cwd` stripped from respond: `✖ respond and every wait chunk ...` (5/4/1). `herd-next`'s guard stripped: `✖ herd-next's gate-mode read carries the same guard ...` (5/4/1). The copy was restored to an empty diff and removed; no file in the task tree was edited. | pass | 0.92 | 0 |
| A10 | Acceptance (d): no line tells a person to run a command (`task.md:13`; claimed: yes, receipts 14 to 18). | `grep -rn 'archon workflow' skills/`, every line read; then the gate templates | No hit addresses a person. | 14 lines in 5 files, all commands a skill runs itself; `deliver_archon_answer.md:7` sits under `Started from the project root:` as past-tense provenance; `deliver_gate_answer.md` and `herd_next_gate_answer.md` both end `Say `approve`, or say what should change.`. `task.md`'s literal glob `skills/*/references skills/*/SKILL.md` matches nothing at all (the skills are one directory deeper). | pass | 0.86 | 0 |
| A11 | Acceptance (f): prove (c) against a real paused run and record the observed JSON and the respond call (`task.md:13`; claimed: no, receipt 18 records it as not executed). | Two scratch `delivery-start` runs on a fresh repository with a bare remote, `--input skills_dir=/nonexistent-skills-dir` | Both the JSON and the respond call are recorded in the verification and receipt artifacts. | Both runs paused at `confirm` within 20s; the JSON and both respond calls are in A3, A4, and A6 of this artifact. Receipt 09 holds the same shape from plan 04's phase 5; receipt 18 holds none. | pass | hand | 0 |
| A12 | Desired End State: `workflows/delivery.md` states who removes an abandoned run's worktree (`13-plan:38`; claimed: yes, receipt 17). | `grep -n 'archon/workspaces' workflows/delivery.md` | One line, in the Dead run bullet. | `:151` — "An abandoned run's worktree stays under `~/.archon/workspaces/`: Archon owns its workspace directories and no skill removes one ... Remove one with `git worktree remove <path>` once its branch is merged or dropped." | pass | hand | 0 |
| A13 | Both readers carry the same guard, and `herd-next` names the skipped reply (`13-plan:371`, `15-implementation:61`; claimed: yes, receipt 15). | `grep -n 'archon workflow get "$run_id" --json) || { printf'` over both skills; `grep -n 'the run could not be read'` | Two identical guard lines; one reason line naming an existing template. | `deliver/SKILL.md:70` and `herd-next/SKILL.md:91`, identical apart from `deliver`'s three-space list indent; `herd-next/SKILL.md:100` names `references/herd_next_skipped_answer.md`, which exists. | pass | 0.92 | 0 |
| A14 | The pane primitives `herd-next`'s gate mode calls exist in the installed Herdr (`herd-next/SKILL.md:110-115`; claimed: no). | `herdr agent --help`, `herdr pane --help`, `herdr notification --help` | `notification show`, `agent start`, `agent prompt`, `agent wait`, `pane rename` are all present. | All five listed by the installed `herdr` at `/opt/homebrew/bin/herdr`. | pass | 1.00 | 0 |
| A15 | Plan 13 checklist steps 3 and 4, the Herdr half, recorded in the phase receipt (`13-plan:332-333`, `13-plan:39`; claimed: no). | Not in this environment: same reason as A1. | The pane label and first message recorded verbatim in receipt 18. | Receipt 18 records the checklist as not executed and holds no observed output for steps 3 and 4. | untested | hand | 0 |

Verdicts: `pass`, `fail`, or `untested` (nothing in this environment could decide it). Confidence is the helper's probability for the verdict, or `hand` when decided without it; deterministic verdicts (an exit code, an exact string) record `1.00`. Severity: 0 none, 1 cosmetic, 2 functional, 3 blocking.

## Findings

None.

## Missing

None.

## Human Review

### Review targets

- The items table, and in particular A3, A4, A6, A7, and A8: the live-run evidence receipt 18 deferred is now recorded here, and it closes plan 13's open question about `--cwd` on `respond`.
- `skills/delivery/deliver/SKILL.md:78`. The behavior is right and the guard stops the loop, but the stated cause is not what the CLI does: `archon workflow get --json` outside a git work tree prints an `ok: false` body whose newlines are escaped, `jq` parses it without error, and `$status` becomes the string `null`, not empty. The same sentence is restated in plan 13 and in receipts 14 and 16. Decide whether to correct it.
- `skills/delivery/start-epic-delivery/references/epic_delivery_final_answer.md`. Its wave list says "run each from the project root" about `{child_start_command}` before its last paragraph says the commands "are printed as the record of what runs, not as something for you to type". Acceptance (d)'s grep cannot see it (the template holds a placeholder, not the literal `archon workflow`), and plan 13 reads (d) as gate steering only, so no item failed on it. It is the one place left where the task's whole-flow rule and the shipped text disagree.
- Acceptance (d) as written in `task.md:13`. Its glob is `skills/*/references skills/*/SKILL.md`, which matches no file in this repository, so the criterion passes vacuously; the check that means anything is `grep -rn 'archon workflow' skills/`, read line by line.

### Verify

- [ ] `npm test`; it exits 0 with `tests 69 / pass 69 / fail 0`.
- [ ] `node scripts/check-commits.mjs main..HEAD --title "feat(delivery): open the next delivery phase in a Herdr pane"`; it exits 0 with `ok: 66 subjects`.
- [ ] A1 and A2: run plan 13's checklist steps 1 to 4 in a live Herdr workspace (`HERDR_ENV` is `1` on this machine, so both halves can be run here) and confirm the pane opens at the run's `working_path`, labelled `<slug>/<phase> gate`, with its agent announcing the pause unprompted and no `archon` command in any reply.
- [ ] A15: record the pane label and first message from those steps in receipt 18, edited in place per the Iteration convention.
- [ ] Re-decide A11: the helper returned `unclear` (0.71). Acceptance (f) asks for the evidence in "the verification and receipt artifacts"; this artifact has it and receipt 09 has the earlier shape, while receipt 18, the phase that owns the checklist, has none.
- [ ] Re-decide A12: the helper returned `unclear` (0.70); `grep -n 'archon/workspaces' workflows/delivery.md` prints exactly the one line quoted above.
- [ ] Re-decide T4: the helper returned `unclear` (0.61); read the `TYPESAFE_API_KEY_FILE` line in `tests/build-packs.test.mjs` and confirm it tightens the no-key case rather than relaxing it.

### Known limits

- A1, A2, and A15 are `untested`. The pane half of acceptance (a) and the session half of acceptance (b) are agent behavior, not commands: deciding them means starting a real delivery run and opening a review pane with an agent in the person's live Herdr workspace, which this session was not asked to do. Their CLI half (A3, A4, A6, A8) and their template half (A10, A14) are decided.
- A11, A12, and T4 came back `unclear` from the typed-judgment helper and were decided by hand; each has a `### Verify` checkbox above.
- `deliver/SKILL.md:78` misstates the cause of the failure its guard prevents (see Review targets). Behavior is unaffected, so no item failed on it.
- The branch carries three earlier tasks as well as this one, so `git diff main...HEAD` is 90 files and four of the five changed test files (`judge`, `commits`, `build-packs`, `typesafe-stub`) belong to that earlier work; they were still read and graded as T items.
- The tree has two untracked paths that predate this session, `.backups/` and `.ignore`; no tracked file is modified. The checks ran against the tree as it is.
- Live evidence cost four Archon runs (`6bd7f01d`, `6d86fa7a` and the two children they dispatched, `95abc080` and `4d12c943`). All four were abandoned, and the scratch repository, its bare remote, and the Archon workspace directory they created were removed. The temporary worktree used for A9's reverts was removed with `git worktree remove`.
- `tests/steward.test.mjs` runs the fences against a fake `archon`, so `npm test` proves the loop's argv and branching, not the CLI's behavior; the CLI's behavior is proved only by A3, A4, A6, A7, and A8, which are one session's live observations, not a check that runs again.
- A1, A2, and A15 are still `untested` after the code-review-fixes pass that raised CR-003 (`20-code-review-steer-every-archon-gate.md`). `HERDR_ENV` is `1` and Archon is installed in that session too, so the CLI half of the checklist could have run again, but opening a review pane and starting an agent in the person's live workspace is the one action this collection's own conventions require confirming first (`shared/CONVENTIONS.md`, and this item's own `### Verify` note above). The fixes pass asked rather than assumed the go-ahead; see `21-code-review-fixes-steer-every-archon-gate.md` for the disposition. The residual risk is unchanged from the row above: A1, A2, and A15 stay undecided until that checklist runs once in this workspace.
