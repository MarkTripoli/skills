---
task: compose-delivery-chain-jev
type: verification
summary: "Re-ran the repository's checks and every acceptance item at `5cd8a09` against `main`: `npm test` exits 0 at 67 of 67, the CI commit-subject check passes 44 subjects, `archon workflow test delivery-adaptive` passes its four fixtures, and all five task acceptance criteria hold, including a live `compose` call that skips research and design on a oneshot-shaped request (0.07 / 0.07) and keeps both on a full-shaped one (0.72 / 0.94). One dry run with the decide nodes executing against a local TypeSafe stub showed both halves of criterion (c) at once - research, design, prd and tdd skipped by `when_condition_false` and the execution-plan artifact written with rows reading `no` - which closes the plan's known limit that no pack-level dry run had rendered that table. No item failed; the status is passed. The next phase gets no findings, but two items nothing here could decide (whether the eight samples are the right eight, and a live default-input run that needs `scripts/install.mjs` first) and the unchanged pre-existing `delivery/bugfix/fixtures/reproduced.stubs.yaml` failure stay under Known limits."
status: passed
revision: 5cd8a09
target: main
---

# Verification

## Run

- Revision: `5cd8a09` on `compose-delivery-chain-jev`; tree clean apart from the untracked `.ignore`, which predates this session and is unrelated to the task
- Target: `main` (no pull request, no `base:` in `task.md`, so the repository default branch); merge base `3fafa25`; 96 files changed, 22 of them under `tests/` or a `fixtures/` directory
- Checks from: `package.json` `test` script (`validate.mjs` then `sync-plugin.mjs --check` then `build-packs.mjs --check` then `node --test tests/`) and `.github/workflows/commits.yml`
- Coverage: 22 acceptance items; 20 claimed by a receipt, 2 claimed by none
- Graded by: typed-judgment helper (`grade-steps --kind command` and `--kind diff`), model `jev-1.13.0`, tokens 5044 in / 428 out, 786 in / 39 out, 3963 in / 319 out over three calls; nine rows came back `unclear` and were decided by hand

## Items

| Id | Item | Decided by | Expected | Observed | Verdict | Confidence | Severity |
|---|---|---|---|---|---|---|---|
| C1 | The repository's own suite. | `npm test` | Exits 0 with no failing test. | Exit 0; `ok: 41 skills, 54 answer templates, 20 human-review templates, 4 execution-DAG templates, 2 work-breakdown templates, 0 banned tokens`; `tests 67 / pass 67 / fail 0`. | pass | 1.00 | 0 |
| C2 | The CI commit-subject check (`.github/workflows/commits.yml`). | `node scripts/check-commits.mjs 3fafa25..HEAD` | Exits 0 with every subject accepted. | Exit 0, `ok: 44 subjects`. | pass | 1.00 | 0 |
| T1 | `tests/judge.test.mjs` | `git diff main...HEAD -- tests/judge.test.mjs`, read in this session | The change keeps this check's strength. | +111/-1: two compose tests added (bar and verdict mapping, paired reason, state, stdin form; all eight samples mapped through the stub) plus a compose assertion on the no-key test; one `extract-json` stderr assertion relaxed from `deepEqual` to `match` because stderr now also carries the provenance line added by commit `ae91316` of another task on this branch; exit code and stdout stay exact. No test removed, skipped, or marked `only`/`xfail`/`todo`. | pass | hand | 1 |
| T2 | `tests/packs.test.mjs` | `git diff main...HEAD -- tests/packs.test.mjs`, read in this session | The change keeps this check's strength. | +73/-0: a `runDecideNode` helper that runs the decide node's real bash body, and one test with four cases (confident-no skip with the dimmed flowchart and the `0.05 / <= 0.20` row; rewrite-not-renumber; no-key canonical object; `~` skills_dir reaching the helper). Nothing removed. | pass | 0.93 | 0 |
| T3 | `tests/dispatch.test.mjs` | `git diff main...HEAD -- tests/dispatch.test.mjs`, read in this session | The change keeps this check's strength. | +31/-21: every existing assertion gains the new `pack` field and the helpers gain `explicit`; one pre-existing `gates=none` expectation flipped to `confirm true` alongside commit `1907fa5` of another task on this branch; one new test for explicit pinning and the judged-lean gate rewrite. No test removed or skipped. | pass | 0.80 | 0 |
| T4 | `tests/commits.test.mjs` | `git diff main...HEAD -- tests/commits.test.mjs`, read in this session | The change keeps this check's strength. | +5/-0: three assertions for a `--title`-only invocation (status, the 74-against-72 message, a short valid title). Nothing removed. | pass | 0.92 | 0 |
| T5 | `tests/lib/typesafe-stub.mjs` | `git diff main...HEAD -- tests/lib/typesafe-stub.mjs`, read in this session | The change keeps this check's strength. | +8/-1: an optional `statuses` queue and `retry-after` header so a test can simulate 429, 5xx and a 400; default behaviour unchanged. | pass | 0.88 | 0 |
| T6 | `tests/fixtures/compose-samples.json` (new) | `git diff main...HEAD -- tests/fixtures/compose-samples.json`, read in this session | The change keeps this check's strength. | New, 29 lines: eight samples, four `oneshot` and four `full`, each with its expected skip set, consumed by a committed test that asserts the shape counts and the research/design expectation per shape. | pass | 0.85 | 0 |
| T7 | The five new pack fixtures (`adaptive/{all-phases,skipped,prd-path,helper-unavailable}`, `decide/run`) | `git diff main...HEAD -- .archon/workflows/delivery/adaptive .archon/workflows/delivery/decide`, read in this session | The change keeps this check's strength. | Five new fixtures; `skipped` and `prd-path` give a skipped node no stub at all, so an unused-stub error is what catches a phase that ran anyway. All five pass with no stub missing or unused. | pass | 0.84 | 0 |
| T8 | The three changed `delivery-start` fixtures | `git diff main...HEAD -- .archon/workflows/delivery/start/fixtures`, read in this session | The change keeps this check's strength. | `unattended-oneshot` and `explicit-bugfix` add `explicit`/`pack` and retarget `fail-node` to `adaptive`; `confirm-lean`'s scenario is rewritten from a reject text naming `lean` to a judged `lean` approved at 0.55, and the reject-text pinning it dropped is asserted in `tests/dispatch.test.mjs` instead. No fixture removed. | pass | 0.84 | 0 |
| T9 | `scripts/validate.mjs` | `git diff main...HEAD -- scripts/validate.mjs`, read in this session | The change keeps this check's strength. | +36/-1: check 6b adds two template lists and two loops (one heading plus a mermaid fence in four templates; heading, fence, `Critical path:` line and the exact table header in two) and two counts in the summary. No existing check removed or loosened. | pass | 0.90 | 0 |
| A1 | `npm test` passes (`task.md:17` acceptance (a); claimed: yes). | `npm test` | "`npm test` passes" | Exit 0, `tests 67 / pass 67 / fail 0`; the C1 run. | pass | 1.00 | 0 |
| A2 | `archon workflow test delivery-adaptive` passes its fixtures (`task.md:17` acceptance (b); claimed: yes). | `archon workflow test delivery-adaptive` | "passes its fixtures" | Exit 0, `4 passed, 0 failed` over `prd-path`, `skipped`, `helper-unavailable`, `all-phases`. | pass | 1.00 | 0 |
| A3 | A dry run with stubs shows the skipped phases' nodes skipped and the execution-plan artifact written by the decide node (`task.md:17` acceptance (c); claimed: yes). | `archon workflow run delivery-adaptive --cwd <scratch> --dry-run --exec-code --json --stubs <no decide stubs> --input gates=none`, with the decide nodes answering a local TypeSafe stub | "shows the skipped phases' nodes skipped and the execution-plan artifact written by the decide node" | One run: exit 0, outcome completed, `missingStubs []`, `unusedStubs []`, four stub calls; `research__questions`, `design__cycle/once`, `prd__cycle/once`, `tdd__cycle/once`, `outline__cycle/once`, `app-test__test` all `when_condition_false`; the same run wrote `01-execution-plan-fixture.md` with `class research skipped` through `class app_test skipped` and rows reading `| research \| no \| - \| 0.05 \| <= 0.20 \|`. | pass | hand | 0 |
| A4 | With the key present, a oneshot-shaped request skips research and design and a full-shaped one keeps them (`task.md:17` acceptance (d); claimed: yes). | `printf '<request>' \| node skills/delivery/typed-judgment/judge.mjs compose --json -` for each shape | "returns per-phase probabilities and skips research and design; a full-shaped request keeps them" | Oneshot ("Set the request timeout in config/http.json from 5s to 30s"): research 0.07 skip, design 0.07 skip, every row carrying probability, bar and reason. Full ("Add multi-tenant support..."): research 0.72 run, design 0.94 run. Model `jev-1.13.0`. | pass | 0.82 | 0 |
| A5 | The `create-tdd` and `create-design-discussion` templates render the section and validate (`task.md:17` acceptance (e); claimed: yes). | `node scripts/validate.mjs` plus a read of the four templates | "render the section and validate" | Each of the four templates carries exactly one `### Execution DAG` with a mermaid fence in the section; the two tdd templates also carry `### Engineering Work Breakdown` with a fence, a `Critical path:` line and the byte-exact header; validate exits 0 reporting `4 execution-DAG templates, 2 work-breakdown templates`; seven answer templates carry the `Check:` bullet. | pass | 0.87 | 0 |
| A6 | Every live probe run this task started is abandoned, detached children too (`task.md:17`; claimed: yes). | `archon workflow status --json` before and after, `git worktree list`, `git status --porcelain` | "must be abandoned or cancelled, and its detached child runs too" | The run list is the same two pre-existing runs before and after (`5961728b`, `a30667ac` delivery-tail); dry runs register none. The scratch repositories and both temporary worktrees created here were removed; the tree is unchanged apart from the untracked `.ignore`. | pass | hand | 0 |
| A7 | `compose --json` prints a probability, the bar, a run/skip verdict and a reason per phase plus the autonomy level, and exits 3 printing nothing when unavailable (`04-plan:36`; claimed: yes). | the two live calls of A4, plus `printf ... \| node judge.mjs compose --json -` with `TYPESAFE_API_KEY` unset | "prints, for each of the eight optional phases, a probability, the bar ..., a run/skip verdict, and a one-line reason, plus the autonomy level; it exits 3 printing nothing" | JSON keys `phases, autonomy, autonomy_suggested, autonomy_confidence, artifacts`; each row `{phase, verdict, probability, bar: 0.2, reason, reason_confidence}` for all eight phases. Unset key: stdout empty, stderr `judge: unavailable: TYPESAFE_API_KEY is not set`, exit 3. | pass | 0.85 | 0 |
| A8 | The sample set and the live probe exist and score every sample (`04-plan:37`; claimed: yes). | `node evals/compose-probe.mjs` with the key | "holds the eight requests ... and prints what a live model scored each one" | Eight samples in the fixture; the probe printed one row per sample and exited 1 on the two the plan already records: `flag-stated-behavior` 0.43/0.59 and `one-function-fix` 0.70/0.19, both within 0.02 of the recorded values; `copy-change` and `config-edit` pass and all four full shapes pass. | pass | hand | 0 |
| A9 | `delivery-adaptive` runs compose at four boundaries, each optional node keyed on the nearest preceding decide field, joins tolerate the skip, canonical order unchanged (`04-plan:38`; claimed: yes). | a read of `.archon/workflows/delivery/adaptive/delivery-adaptive.yaml`, node by node | "each optional phase node carries `when:` on the nearest preceding decide node's field, every join after one tolerates the skip, and the canonical order is unchanged" | 31 nodes matching the plan's table row for row: four `delivery-decide` includes each followed by a plain bash join; `when:` on `decide-task`, `decide-research` (design, prd, tdd), `decide-design` (planning plan/outline), `decide-plan` (app_test); `trigger_rule: none_failed_min_one_success` on every join after a guarded node; order research, design, prd, tdd, plan/outline, implement, verify, app-test, review, pr. | pass | hand | 0 |
| A10 | Each decide node writes or rewrites one execution-plan artifact with dimmed skips and the full table (`04-plan:39`; claimed: yes). | the A3 run's artifact, read in this session | "a Mermaid flowchart with skipped phases dimmed and a table of every probability, bar, reason, and model tier" | One `01-execution-plan-fixture.md` rewritten at all four boundaries; frontmatter `type: execution-plan` with `boundary`; `classDef skipped` plus a `class <id> skipped` per skipped phase; `Gates requested:` line; eleven-row table with `Phase, Runs, Gate, p, Bar, Why, Model tier` and tiers taken from the `tiers` input. | pass | 0.80 | 0 |
| A11 | The validator fails a template missing either section (`04-plan:40`, Phase 4 criterion; claimed: yes). | the heading renamed in a throwaway worktree of HEAD, then `node scripts/validate.mjs` | "`scripts/validate.mjs` fails a template missing either" | Renaming `### Engineering Work Breakdown`: exit 1, `must contain exactly one "### Engineering Work Breakdown" heading (found 0)`. Renaming `### Execution DAG`: exit 1 with the matching message. Restored copy: exit 0 with both counts. No file in the repository was touched. | pass | 1.00 | 0 |
| A12 | `node scripts/build-packs.mjs --check` is clean, so the `-omp` flavor is not stale (`04-plan:41`; claimed: yes). | `node scripts/build-packs.mjs --check` | "is clean" | Exit 0, no output. | pass | 1.00 | 0 |
| A13 | `returns:` resolves for a bash node with an `output_format` object (Phase 2 criterion, `04-plan:435`; claimed: yes). | `archon workflow run delivery-decide --cwd <scratch> --dry-run --exec-code --json --input task_dir=...` | "prints node `decide` with the declared object" | Outcome completed; node `decide` completed with `{"research":"true",...,"autonomy":"all","available":"false"}` and wrote `01-execution-plan-decide-fixture.md`. | pass | 1.00 | 0 |
| A14 | `archon workflow test delivery` passes, with the pre-existing `bugfix` fixture recorded (Phase 3 criterion, `04-plan:614`; claimed: yes). | `archon workflow test delivery`, then the same command in a detached worktree at `3fafa25` | "53 of 54 pass ... the one failure is `delivery/bugfix/fixtures/reproduced.stubs.yaml`" | Exit 1, `53 passed, 1 failed`; the failure is `delivery/bugfix/fixtures/reproduced.stubs.yaml -> delivery-bugfix (failed) / expected completed, dry-run reported failed`. At `3fafa25`: `3 passed, 1 failed` on the same fixture with none of this branch present, and `git diff --name-only 3fafa25..HEAD` over both bugfix directories is empty. Every adaptive and decide fixture passes. | pass | hand | 1 |
| A15 | Without a key the probe reports unavailable rather than passing vacuously (Phase 1 criterion, `04-plan:288`; claimed: yes). | `node evals/compose-probe.mjs --json >/dev/null` with `TYPESAFE_API_KEY` unset | "exit 3" | Exit 3 with `judge: unavailable: TYPESAFE_API_KEY is not set`. | pass | 1.00 | 0 |
| A16 | A `~` in `skills_dir` resolves in the decide node, so the pack consults JEV in its shipped configuration (plan `### Known limits`, `04-plan:880`; claimed: yes). | `node --test --test-name-pattern "decide node" tests/packs.test.mjs` plus a read of `delivery-decide.yaml:48` | "This limit no longer holds." | The `case "$skills" in "~") ... "~/"*) ...` expansion is present at `delivery-decide.yaml:48`, copied from the task node; the focused test passes 1 of 1 including the case asserting `available` is `"true"` for a `~`-prefixed directory. | pass | 1.00 | 0 |
| A17 | `delivery-start` sends a judged oneshot/lean/full/prd to `delivery-adaptive` while `--input workflow=` pins a fixed pack and `task.md` keeps the judged pack (`task.md:13`; claimed: yes). | `node --test tests/dispatch.test.mjs` | The judged route resolves `pack=adaptive`, an explicit pick resolves the named pack, a judged lean's `outline` gate becomes `plan`. | `tests 8 / pass 8 / fail 0`, including `pack":"adaptive"` for the judged route, `pack":"full"` for `--input workflow=full`, `pack":"lean"` for a reject text naming lean, and `gates":"plan"` for the judged lean. | pass | 1.00 | 0 |
| A18 | The artifact type is documented in `shared/CONVENTIONS.md` and the pack in `workflows/delivery.md` and `docs/cheatsheet.md` (`task.md:15`; claimed: yes). | `grep -c` over the three files plus `node scripts/validate.mjs` check 10 | "Document the artifact type in shared/CONVENTIONS.md and the pack in workflows/delivery.md and docs/cheatsheet.md" | `shared/CONVENTIONS.md` names `NN-execution-plan-<slug>.md` (`type: execution-plan`) as the artifact a pack's decide node writes; `workflows/delivery.md` mentions `delivery-adaptive` on 6 lines and `delivery-decide` on 5; `docs/cheatsheet.md` on 2; validate's check 10 passes. | pass | hand | 0 |
| A19 | When JEV is unavailable the decide node outputs the canonical full chain (`task.md:13`; claimed: yes). | the A13 run with `TYPESAFE_API_KEY` unset | "the decide node outputs the canonical full chain" | Every phase `"true"`, `planning plan`, `implement_skill implement-plan`, `autonomy all`, `available "false"`, and an artifact whose probability and bar columns are all `-`. | pass | 1.00 | 0 |
| A20 | Whether the eight committed samples are the right eight (plan `## Human Review`, `04-plan:863`; claimed: no). | not in this environment: a judgment about whether these requests represent the shapes the wording must separate | The four oneshot shapes really are oneshot-shaped and each full shape really needs research and design. | Not decided here. | untested | hand | 0 |
| A21 | The per-boundary collapse does not happen with this task's artifacts in the state (plan `### Known limits`, `04-plan:881`; claimed: yes). | `node skills/delivery/typed-judgment/judge.mjs compose --json .agents/tasks/compose-delivery-chain-jev` with the key | "research scored 0.75 and design 0.96 against a bar of 0.2, both runs; only `app_test` skipped" | With 14 artifact summaries in the state: research 0.65 run, design 0.95 run, prd 0.22 run, tdd 0.65 run, plan 0.92 run, outline 0.39 run, review_each_phase 0.66 run, app_test 0.15 skip. The limit holds exactly as recorded. | pass | hand | 0 |
| A22 | A live default-input `delivery-adaptive` run reaching the installed helper (receipt `12`, Deferred Human Evidence; claimed: no). | not in this environment: it needs `node scripts/install.mjs` to overwrite the user's installed `~/.agents/skills`, an action outside this change | A decide node reaches `judge.mjs compose` at the default `~/.agents/skills`. | Not decided here; the installed helper still predates this branch and has no `compose` command. | untested | hand | 0 |

Verdicts: `pass`, `fail`, or `untested` (nothing in this environment could decide it). Confidence is the helper's probability for the verdict, or `hand` when decided without it; deterministic verdicts (an exit code, an exact string) record `1.00`. Severity: 0 none, 1 cosmetic, 2 functional, 3 blocking.

## Findings

None.

## Missing

None.

## Human Review

### Review targets

- The items table above, then the Known limits list; there are no findings and nothing is missing.
- `A3` and `A10`: the single dry run that rendered an execution-plan table whose rows read `no`. It contradicts the plan's known limit that only the fallback path had been seen at pack level, and it is the clearest evidence that the decide node's judgment, dimming and table agree.
- `T1` and `T3`: the two test files whose existing assertions changed. Both changes ride along from other tasks committed on this same branch (`ae91316`, `1907fa5`), not from this task's phases.

### Verify

- [ ] Run `npm test`; it exits 0 with `ok: 41 skills, 54 answer templates, 20 human-review templates, 4 execution-DAG templates, 2 work-breakdown templates, 0 banned tokens` and `tests 67 / pass 67 / fail 0`.
- [ ] Run `node scripts/check-commits.mjs 3fafa25..HEAD`; it exits 0 with `ok: 44 subjects`.
- [ ] Re-decide `T1`: read the `extract-json` assertion in `tests/judge.test.mjs` and decide whether matching stderr is enough now that the provenance line shares that stream, or whether it should assert both lines exactly.
- [ ] Re-decide `A3`: rerun the adaptive dry run with the decide nodes unstubbed against a local TypeSafe stub; it completes with `missingStubs []` and writes an execution-plan artifact whose research, design, prd and tdd rows read `no`.
- [ ] Re-decide `A6`: `archon workflow status --json` lists only the two pre-existing `delivery-tail` runs and `git worktree list` shows no verification worktree.
- [ ] Re-decide `A8`: rerun `node evals/compose-probe.mjs` with the key; six of eight samples pass and the two failures are `flag-stated-behavior` and `one-function-fix`.
- [ ] Re-decide `A9`: read `delivery-adaptive.yaml` and confirm the four decide includes, the single-equality `when:` on the nearest preceding decide node, and `trigger_rule: none_failed_min_one_success` on every join after a guarded node.
- [ ] Re-decide `A14`: `archon workflow test delivery` reports `53 passed, 1 failed` on `delivery/bugfix/fixtures/reproduced.stubs.yaml`, and the same command at `3fafa25` reports `3 passed, 1 failed` on the same fixture.
- [ ] Re-decide `A18`: `grep -c 'delivery-adaptive' workflows/delivery.md docs/cheatsheet.md` prints 6 and 2, and `shared/CONVENTIONS.md` names the `execution-plan` artifact type.
- [ ] Re-decide `A19`: rerun the decide block with `TYPESAFE_API_KEY` unset; it prints the canonical object with `"available":"false"`.
- [ ] Re-decide `A21`: rerun `compose --json` against this task directory; research and design still score above the bar.
- [ ] Decide `A20`: whether the eight samples in `tests/fixtures/compose-samples.json` are the right eight.
- [ ] Decide `A22`: run `node scripts/install.mjs`, then a live `delivery-adaptive` run with default inputs, and watch a decide node reach the installed `judge.mjs compose`.

### Known limits

- `A20` is untested: whether the eight committed samples are the right eight is a judgment about request shapes, not something a command decides.
- `A22` is untested: it needs `node scripts/install.mjs` to overwrite the user's installed `~/.agents/skills`, an action outside this change, so the tilde fix is proven by the unit test rather than end to end.
- Nine rows came back `unclear` from the typed-judgment helper (`T1`, `A3`, `A6`, `A8`, `A9`, `A14`, `A18`, `A19`, `A21`) and were decided by hand against the recorded output; each carries a `### Verify` checkbox above.
- `archon workflow test delivery` still exits 1 on `delivery/bugfix/fixtures/reproduced.stubs.yaml`. Reproduced here at the merge base `3fafa25` with none of this branch present, and `git diff --name-only 3fafa25..HEAD` over both bugfix directories is empty, so the branch neither caused it nor touches that pack. It is not part of acceptance (a), which is `npm test`, and `npm test` does not run that command.
- Two of the four oneshot-shaped samples still miss the bar on a live probe (`flag-stated-behavior` research 0.43 design 0.59, `one-function-fix` research 0.70), the same two the plan records with the same probabilities. Acceptance (d) is met by copy-change- and config-edit-shaped requests, not by every oneshot shape.
- On the oneshot-shaped acceptance (d) request, `prd` (0.44) and `tdd` (0.43) both ran, so a config-edit-shaped task would still get those two phases. Nothing in the acceptance criteria constrains the six phases other than research and design, and no sample measures them.
- The plan's known limit that no pack-level dry run had rendered an execution-plan table whose rows read `no` no longer holds: the `A3` run rendered one, on the judged path, with the decide bodies executing.
- The per-boundary collapse the design expects still does not happen (`A21`): with 14 artifact summaries in the state, research and design score 0.65 and 0.95 and only `app_test` skips. This is the plan's own recorded limit, confirmed rather than newly found.
- The tree carries the untracked `.ignore` file, which predates this session and is unrelated; the checks ran against the tree as it is.
- The diff against `main` also carries two other tasks' work committed on this branch (`i-m-curious-what`, `describe-pr-title-rule`). Each has its own verification artifact in its own task directory; the test files they touched are recorded here as `T1`, `T3`, `T4` and `T5` because they appear in this branch's diff.
