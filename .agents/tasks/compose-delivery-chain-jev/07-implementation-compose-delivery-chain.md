---
type: implementation
completed_phase: 3
summary: "Phase 3 ships `.archon/workflows/delivery/adaptive/delivery-adaptive.yaml`, the 31-node pack from plan 3.1 exactly: four `delivery-decide` includes at the task, research, design, and plan boundaries, a plain bash commit join after each (Archon note 1), every optional phase carrying a single-equality `when:` on the nearest preceding decide node's field, and `trigger_rule: none_failed_min_one_success` on every join that follows one. Four fixtures prove the all-phases, skipped, prd-outline, and helper-unavailable branches, and a dry run against the `skipped` stubs shows `research__questions` as `skipped | when_condition_false` with no stub missing or unused. `delivery-start` gains `explicit` on `route` and `pack` on `resolve`, so a judged (non-explicit) oneshot, lean, full, or prd run continues in `delivery-adaptive` while `task.md` still records the judged pack, and a judged lean's `outline` gate is rewritten to the `plan` gate the adaptive outline node carries; the rewrite is a portable token loop because the plan's `sed \\b` is a GNU extension BSD sed silently no-ops. Two of the six Automated Verification boxes stay open on failures reproduced on unmodified `ff7bdb4` — `delivery/bugfix/fixtures/reproduced.stubs.yaml` and `tests/install.test.mjs`'s missing `@clack/prompts` — which Phase 6 owns as acceptance (a). Phase 4 depends on nothing here."
---

# Implementation Receipt

## Source
- task: `.agents/tasks/compose-delivery-chain-jev/task.md`
- plan artifact: `.agents/tasks/compose-delivery-chain-jev/04-plan-compose-delivery-chain.md`
- phase range: Phase 3 only (The `delivery-adaptive` pack and the `delivery-start` route)

## Child Workers
- implementer: `agent-implementer` (`phase3-implementer`), one run, final message verified against the repository
- reviewer: none for this phase; this receipt is the phase's review artifact

## Completed Work

- `.archon/workflows/delivery/adaptive/delivery-adaptive.yaml` (new): inputs `skills_dir`, `task_dir`, `gates` (`default: all`, names `design prd tdd plan phases pr`), `app_test`, `app_target`, `verify`, `interactive: true`, and no `review_each_phase` input. The node list matches plan 3.1's table row for row — verified id, `depends_on`, and `when:` against all 31 rows — including the four `decide-*` includes each followed by a plain `bash:` commit join, `plan` and `outline` as siblings off `decide-design-done` under `planning == 'plan'` / `== 'outline'`, `implement` with no `when:`, and `trigger_rule: none_failed_min_one_success` on every join that follows a `when:`-guarded or gated node.
- `.archon/workflows/delivery/adaptive/fixtures/{all-phases,skipped,prd-path,helper-unavailable}.stubs.yaml` (new): the four fixtures of plan 3.2, each stubbing the decide nodes by namespaced id (`decide-task__decide` and its three siblings). `skipped` and `prd-path` give the skipped nodes no stub entry at all, so their absence is what proves the skip; `archon workflow test` reports `missingStubs` and `unusedStubs` both empty.
- `.archon/workflows/delivery/start/delivery-start.yaml`: `route` emits `explicit` (true when `INPUTS_WORKFLOW` is not `auto`); `resolve` computes `pack` (`adaptive` only for a judged, non-explicit `oneshot`/`lean`/`full`/`prd`; any reject text naming a pack pins that fixed pack), rewrites an `outline` gate to `plan` when `pack=adaptive` and swaps in the adaptive gate-name set, and adds `pack` to the printed object and `required`. Every pack node's `when:` moves from `$resolve.output.workflow` to `$resolve.output.pack`, a new `adaptive` `workflow:` node is added with `app_test`/`app_target`, and `done` depends on it. `task` still takes `$resolve.output.workflow`, so `task.md` never records `adaptive`.
- `.archon/workflows/delivery/start/fixtures/{unattended-oneshot,confirm-lean,explicit-bugfix}.stubs.yaml`: carry `explicit` and `pack`; the first two now fail at `adaptive` instead of `oneshot`/`lean`.
- `tests/dispatch.test.mjs`: `routed()` and `resolve()` carry `explicit`; the existing resolve assertions gain `pack`; a new test covers `--input workflow=full` resolving `pack=full` and a judged `lean` at autonomy `plan` resolving `gates=plan` with `pack=adaptive`. All four cases plan 3.3 names are covered across the two tests.
- `.archon/workflows/delivery-omp/{adaptive,start}/` (generated): regenerated by `node scripts/build-packs.mjs`.
- Pre-edit copies of the five existing files under `.backups/phase3/`, plus the plan artifact; `.backups/` and `.ignore` stay untracked and unstaged.

Four deviations carried from the child and accepted:

1. The gate rename is a portable per-token loop, not plan 3.3's `sed -e 's/\boutline\b/plan/g'`. `\b` is a GNU extension; BSD/macOS `sed` silently leaves `outline` unchanged, which breaks the judged-lean case outright. The loop produces the identical result on both and only ever matches a whole gate name. The new `tests/dispatch.test.mjs` case is what catches a regression here.
2. `confirm-lean.stubs.yaml`'s scenario is rewritten, not relabeled. Plan 3.3 says it should reach `adaptive`, but its original scenario is a reject text naming `lean`, which by the plan's own Human Review decision 2 always pins the fixed pack. The fixture now stubs a judged `lean` at confidence 0.55 that the reviewer approves — non-explicit, so it genuinely resolves to `pack=adaptive` with `gates=plan`, which is the rule the fixture was meant to demonstrate.
3. Acceptance checkbox 6's command is corrected (see Automated Verification). The plan's literal `--default-stubs` form cannot reach the decide node at all.
4. Editorial cleanup of `delivery-start.yaml`'s description and header comment beyond the plan's literal diff; same substance, no behavior change.

## Automated Verification

- command: `node scripts/build-packs.mjs && node scripts/build-packs.mjs --check`
- result: pass
- evidence: exit 0, no diff after the generated `delivery-omp/adaptive/` and `delivery-omp/start/` files are written

- command: `node scripts/validate.mjs`
- result: pass
- evidence: `ok: 41 skills, 54 answer templates, 20 human-review templates, 0 banned tokens, packs checked with /usr/local/bin/archon`

- command: `archon workflow test delivery-adaptive`
- result: pass
- evidence: `4 passed, 0 failed` — `prd-path`, `skipped`, `helper-unavailable`, `all-phases`, each `→ delivery-adaptive (completed)`

- command: `sed '/^fixture:/,$d' .archon/workflows/delivery/adaptive/fixtures/skipped.stubs.yaml > /tmp/adaptive-skipped.stubs.yaml && archon workflow run delivery-adaptive --dry-run --json --stubs /tmp/adaptive-skipped.stubs.yaml --input task_dir=.agents/tasks/fixture --input gates=none`
- result: pass
- evidence: `outcome: completed`, `missingStubs: []`, `unusedStubs: []`, and the trace shows `research__questions skipped when_condition_false`, `research__research skipped trigger_rule`, with `design__cycle`, `prd__cycle`, `tdd__cycle` likewise `when_condition_false` and `plan__once` / `implement__phases-auto` reached. The plan's literal `--default-stubs` form was run first and fails before `decide-task`: `'$task__create.output.task_dir' references field 'task_dir', but node 'task__create's output is not a JSON object`, because a default stub is unstructured text. The plan's checkbox text carries the same correction. Nothing is written — `exec-code` stays off and no `.agents/tasks/fixture` directory is created.

- command: `archon workflow test delivery`
- result: 53 of 54 pass; the single failure is pre-existing and unrelated
- evidence: `✘ delivery/bugfix/fixtures/reproduced.stubs.yaml → delivery-bugfix (failed) / expected completed, dry-run reported failed`. Reproduced identically in a clean `git clone` of `ff7bdb4`, the commit before this phase, which carries none of these changes. Every fixture Phase 3 adds or touches passes. The checkbox stays open.

- command: `node --test tests/`
- result: 56 of 57 pass; the single failure is pre-existing and unrelated
- evidence: `tests 57 / pass 56 / fail 1`, the failure `tests/install.test.mjs` with `ERR_MODULE_NOT_FOUND: Cannot find package '@clack/prompts' imported from scripts/install.mjs`. `@clack/prompts` is declared in `package.json` and absent from `node_modules`; this is the same uninstalled-dependency gap the Phase 1 and Phase 2 receipts recorded. The checkbox stays open.

- command: `node --test tests/dispatch.test.mjs`
- result: pass
- evidence: `tests 8 / pass 8 / fail 0`

## Deferred Human Evidence

- None. Phase 3 declares no deferred item and `human-gated: false`.

## Commit Handoff

The phase commit `cdd1557 feat(delivery-adaptive): compose the chain at every phase boundary` was created after every check above was run, with `git add` on explicit code paths only (`.archon/workflows/delivery/adaptive/`, `.archon/workflows/delivery-omp/adaptive/`, `.archon/workflows/delivery/start/delivery-start.yaml`, its three touched fixtures, `.archon/workflows/delivery-omp/start/`, `tests/dispatch.test.mjs`). Four of the six Automated Verification boxes are ticked; the two open ones are the pre-existing `delivery-bugfix` fixture and `tests/install.test.mjs` failures above, both reproduced on unmodified `ff7bdb4` and both owned by Phase 6 as task acceptance (a). No Phase 3 file is implicated in either, so the phase commit was not held. The ticked plan and this receipt are committed separately as `docs(task): implementation artifact`.

## Human Review

### Review targets

- `.archon/workflows/delivery/adaptive/delivery-adaptive.yaml`, especially the `with:` blocks the plan spelled out (`decide-task`'s `tiers` string, `outline`'s `gate: $gates.output.plan`, `implement`'s `skill: $decide-design.output.implement_skill` and `review: $decide-plan.output.review_each_phase`, `app-test`'s `kind:`), and the commit bodies on the four `decide-*-done` joins.
- The `resolve` body in `delivery-start.yaml`: the `explicit` / `pack` derivation and the gate rename loop (deviation 1). This is the only place a person's explicit pack choice is distinguished from a judged one, and getting it backwards silently routes every hand-named pack into the adaptive chain.
- `confirm-lean.stubs.yaml`'s rewritten scenario (deviation 2) — whether a judged-and-approved `lean` is the case worth locking in there, or whether the reject-text case deserves its own fixture instead.

### Verify

- Rerun the six Success Criteria commands. Four are green; the two open ones fail only on `delivery/bugfix/fixtures/reproduced.stubs.yaml` and `tests/install.test.mjs`, both reproducible on a clean clone of `ff7bdb4`.
- Confirm the skip is observable, not just asserted by a fixture: the dry run above prints `research__questions | skipped | when_condition_false` with `missingStubs` and `unusedStubs` empty, so no node was skipped for want of a stub.
- Confirm `task.md` can never record `adaptive`: the `task` node in `delivery-start.yaml` still reads `$resolve.output.workflow`, and only the pack nodes read `$resolve.output.pack`.

### Known limits

- No fixture exercises the decide node's own bash under the adaptive pack: all four set `exec-code: false` and stub `decide-*__decide` by namespaced id, so the block's body is proven by Phase 2's own test, not here. The execution-plan artifact is therefore not written in any Phase 3 run.
- In a dry run, an `include:` whose `when:` reads another node's output completes its nested `loop_group` without descending into the body, so the body's first node reports `skipped`/`trigger_rule` while the loop itself reports `completed`. The child reproduced this against untouched `delivery-full`, so it is an Archon 0.10.1 engine limitation, not a property of this pack; it is why the fixtures' `reached:` lists name the loop, not the phase body.
- `delivery-adaptive` is reachable only through `delivery-start`'s judged path or by being run directly; no fixed pack was retrofitted, per What We're NOT Doing.
- Run directly with no `task_dir`, the task node writes `workflow: full`; nothing asserts that floor yet.
- Acceptance criterion (a) is still not clean, on the same two pre-existing failures. Phase 6 settles it.
- This phase started no live workflow run. Every command above was a dry run or a local test; no scratch repository, no detached child, nothing to abandon under the task's live-probe rule.
