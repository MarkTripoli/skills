.DS_Store
dist/
node_modules/

# Backups made before large file changes, per CLAUDE.md.
.backups/

# Personal ripgrep preference (re-admits gitignored graft/ cards to search); not shared.
/.ignore

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Eval runs: transcripts, answers, and built skills from `npm run evals`.
/evals/results/
