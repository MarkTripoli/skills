---
"@marktripoli/skills": minor
---

Add the standalone `jev-ui` skill for bounded browser and Android emulator control. Live native use requires the documented external drivers and an explicitly selected target; model-backed runs require TypeSafe credentials and a configured text helper when text entry is needed.
