---
"@marktripoli/skills": patch
---

The implementation loop ends when every phase box is ticked whatever `judge.mjs plan-remaining` answers; the helper's `done` verdict can still end it early when boxes lag receipts. A confident but wrong `remaining` on a finished plan had kept the loop running sixteen no-op sessions and failed the run before verification. The question now also names deferred human evidence, `human-gated` fields, and success-criteria commands as not remaining work.
