---
"@marktripoli/skills": minor
---

`/deliver` starts the routed `archon workflow run` instead of printing it and reports the run id, branch, and first pause; a request that says "print the command" gets the command alone. The by-hand chain without Archon is unchanged.
