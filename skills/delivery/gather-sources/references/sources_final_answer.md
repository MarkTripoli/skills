Artifact saved: {artifact_link}

Summary:
{summary}

Known limits:
{known_limits}

Later phases select current `research.sources` through `index.json` instead of fetching the sources again.

Next action:
Open a new session in {run_location}, then run:

```text
{next_command}
```
