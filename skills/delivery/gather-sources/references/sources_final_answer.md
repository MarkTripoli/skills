Artifact saved: {artifact_link}

Summary:
{summary}

Known limits:
{known_limits}

Later phases select this artifact by its `type: sources` frontmatter and read it in place of fetching the sources again.

Next action:
Open a new session in {run_location}, then run:

```text
{next_command}
```
