{summary}

Artifact saved: {artifact_link}

Needed:
- {needed}

Verification remains blocked because the repository checks could not run. Supply what the `## Missing` list names, then run `/verify-implementation` again; it records the next `review.verification` iteration.
