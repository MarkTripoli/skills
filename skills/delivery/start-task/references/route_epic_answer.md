Route: epic. {reason}

Task created: `{task_dir}/task.md`, the parent of the epic. `create-epic-plan` splits the request into child tasks, each with its own workflow type and dependencies; `start-epic-delivery` then creates the child task directories and starts the first wave. When the codebase is unfamiliar, run `/create-research-questions` and `/create-research` in the parent task first so the plan names real files.

Start the next phase in a new session, or hand the task to `/run-task`; continuing in this session carries this phase's context into the next one.

```text
/create-epic-plan
```
